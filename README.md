# management_master
便民管理系统
#该项目为springboot单体服务系统 
#依赖环境  java8  mysql 
DOI: 10.1109/ICCPCT.2016.7530217


In this paper we attempt to provide unified security and privacy multilateral security architecture for cloud services stack, using key latest technology via LxC in general and Docker containers in specific. Docker is one of the first to use LxC at such a large scale, but LxC is not a very new concept, it is around for quite some time. Docker container service is a key Platform as a Service (PaaS) based engine. Unified layered cloud computing multilateral security implementation addresses the potential risks associated with cloud computing environment. It also specifies when, at what time, where, and how to apply security controls. Unified Design includes the structured connection between the components of cloud ecosystem for basket and bucket of unified cloud-based hybrid services using micro service oriented venture architectural services realized in terms of PAAS using Docker container as a service. We surveyed various industry/academic professionals and based on our experience we tried to understand the challenges they faced while using the cloud system. We tried to understand the security challenges, incidents, story pieces, accidents, experience they faced while designing, implementing and using the cloud services/resources virtually via online. This work helps to assess the security design and architecture quality using multilateral security framework for Docker container. We try to present the architecture using OSI/TCP/IP stack model with reference to Cloud service stack model and deployment stack model and try to propose the balanced multilateral security prototype with reference to Docker Container security architecture.

在本文中，我们试图通过LxC和Docker容器，为云服务堆栈提供统一的安全和隐私多边安全架构。Docker是最早在如此大规模地使用LxC的公司之一，但LxC并不是一个非常新的概念，它已经存在了相当长的时间。Docker容器服务是基于PaaS(Platform as a Service)的关键引擎。统一分层云计算多边安全实施解决了云计算环境相关的潜在风险。它还指定了何时、何时、何处以及如何应用安全控制。统一设计包括云生态系统组件之间的结构化连接，用于统一的基于云的混合服务，使用面向微服务的风险架构服务，以PAAS使用Docker容器即服务实现。我们调查了各种行业/学术专业人员，根据我们的经验，我们试图了解他们在使用云系统时面临的挑战。我们试图了解他们在通过在线虚拟设计、实施和使用云服务/资源时面临的安全挑战、事件、故事片段、事故、体验。本工作有助于评估Docker容器使用多边安全框架的安全设计和架构质量。我们尝试结合云服务堆栈模型和部署堆栈模型，使用OSI/TCP/IP堆栈模型来描述体系结构，并尝试结合Docker容器安全体系结构提出平衡的多边安全原型。

Cloud computing is an online sharing of resources distributed globally among multi-tenants, with virtual computation utilizing Internet for computation purposes. The various types of cloud services offered are-IAAS, PAAS, and SAAS [NIST]. The Cloud deployment model involves Public model, Private model, Hybrid model and Community model. We use the Hybrid community computing model involving Platform as a Service [PAAS] using Docker Container as a service case study for our unified security sculpt as it runs as a central-ware linking Infrastructure as a Service [IAAS] and Software apps as a Service [SAAS].

Multilateral Security Design assessments include shared responsibility among cloud service stakeholders with each facades of “key” depending on the stakeholders described the system objects-including the scheme under development such as structure, behavior, communication with serviceable and Non-functional Requirements [NFR]. Cloud Security Architectural Stack includes front tier which control the user interface utility to use the system's services, Middle tier to-control the applications foremost functionality and Back end tier to control the applications data access and storage [warehouse, mining and processing] maintained in multilateral vendor site. In cloud computing system security is often treated as: not a priority, and no time to market, also security implementation is regarded as a financial burden, with no returns on investment. However, there is no such thing as perfectly secure software! A foolproof security is not possible, and is simply cost prohibitive. Our goal is to make an attacker's job as tough as possible, thus prevent us from being victims in their hands.. 
云计算是多租户之间全球分布的资源的在线共享，虚拟计算利用互联网进行计算。提供的各种类型的云服务有-IAAS、PAAS和SAAS [NIST]。云部署模型包括公共模型、私有模型、混合模型和社区模型。我们使用混合社区计算模型，涉及平台即服务【PAAS】，使用Docker容器作为服务案例研究，用于我们的统一安全雕塑，因为它作为连接基础设施即服务【IAAS】和软件应用即服务【SAAS】的中央软件运行。

多边安全设计评估包括云服务利益相关者之间的分担责任，每个门面取决于描述的系统对象的利益相关者，包括正在开发的方案，如结构、行为、与可服务和非功能需求的通信[NFR]。云安全架构堆栈包括控制用户界面实用程序以使用系统服务的前端层、控制应用程序最重要功能的中间层和控制在多边供应商站点中维护的应用程序数据访问和存储【仓库、挖掘和处理】的后端层。在云计算系统中，安全通常被视为：不是优先事项，也没有时间上市，安全实施也被视为财务负担，没有投资回报。然而，没有完全安全的软件!万无一失的安全是不可能的，而且成本很高。我们的目标是使攻击者的工作尽可能艰难，从而防止我们成为他们手中的受害者。

1.1	Linux Container-(LXC) Description
The Linux Containers (LXC) begins with the hardware and operating system; facet is a trivial light mass, resource, process, service container infrastructure in budding a cloud technology based on speedy Virtualization system that does not engage an individual to set up a virtual machine [VM] on the physical hardware emulation of the cloud infrastructure. LXC feature adds POSIX package file capabilities to execute the process and network in isolation; it acquires the c-groups resource/asset infrastructure supervision facilities as its basis. LXC are secluded by Kernel-Control-Groups (c-groups) and Kernel Name spaces. Earlier versions are BSD Jails, and Solaris Zones. It uses products, tools, and libraries, which make it. We can spin up a container in fractions of a second because booting a VM takes more time. Users can craft his own framework that would implement containers and then features are offered via the kernel. Containers and the orchestration systems are easy to use. Container decoupled from the infrastructure uses the shared kernel. After installing the container package, it is easy to create, start, stop, and connect to other containers. Initialization and closing down a container is quicker than opening and shutting down a full VM . In the next section, we try to define Docker container as a tool using LXC
1.linux说明
Linux容器（LXC）从硬件和操作系统开始；方面是一个微不足道的轻量、资源、进程、服务容器基础架构，在基于快速虚拟化系统的云技术中，该技术不需要个人在云基础架构的物理硬件仿真。LXC功能添加了POSIX软件包文件功能，以单独执行流程和网络；它获取c-groups资源/资产基础架构监督设施作为其基础。LXC被内核控制组（c-groups）和内核名称空间隔离。早期版本是BSD Jails和Solaris Zones。它使用产品、工具和库，这些都使它成为。我们可以在几分之一秒内启动容器，因为引导虚拟机需要更多的时间。用户可以精心制作自己的框架，实现容器，然后通过内核提供功能。容器和编排系统易于使用。与基础架构解耦的容器使用共享内核。安装容器包后，创建、启动、停止和连接其他容器都很容易。初始化和关闭容器比打开和关闭完整虚拟机更快。在下一节中，我们将尝试将Docker容器定义为使用LXC的工具

1.1.1	 Docker Container as a Tool Using LXC
A dot Cloud (PAAS) mission engine, was primarily entrusted on 18th January 2013, Docker 0.1.0 was released on 25th March 2013. An OS level virtualization mode for supervision of various remote Linux systems (containers-LXC containers) on a single host. Dockers with Open stack are open source cloud technologies. Open stack Heat release is used to orchestrate an OS copy which contains Docker and Kubernetes that runs an image, either in VM or on bare metal in a cluster composition. Docker provides a PAAS (or Containers as-a-Service) key for Open Stack. Pluggable container orchestration engines are like (Kubernetes or Docker Swarm as of now) with open stack. Docker Use Linux kernel isolation features to give a VM like environment. Docker helps in Sand boxing of process and/or application. It is a lightweight container virtualization platform. It is an applicationpackaging and delivering technology. Similar Technologies include Marathon, Mesosphere, MesOS, Core OS, Spunn of Docker engine. Micro services with Solaris zones, Lxc, Rkt, lmctfy OpenVC, BSD jails, run instances of OS by taking a very light weight version of OS.

使用LXC作为工具的Docker容器
dot Cloud (PAAS)任务引擎，主要于2013年1月18日委托，Docker 0.1.0于2013年3月25日发布。一种操作系统级虚拟化模式，用于在单个主机上监控各种远程Linux系统（容器-LXC容器）。使用Open stack的Docker是开源云技术。开放堆栈热发布用于编排包含Docker和Kubernetes的操作系统副本，这些副本可以在虚拟机中或集群组合中的裸机上运行映像。Docker为Open stack提供了PAAS（或容器即服务）密钥。可插拔的容器编排引擎就像（Kubernetes或Docker Swarm到现在为止），具有开放堆栈。Docker使用Linux内核隔离功能提供类似虚拟机的环境。Docker有助于对进程和/或应用程序进行沙盒。它是一个轻量级的容器虚拟化平台。它是一种应用程序打包和交付技术。类似的技术包括马拉松、Messphere、MesOS、Core OS、Docker引擎的旋转。具有Solaris区域、Lxc、Rkt、lmctfy OpenVC、BSD监狱的微服务通过使用非常轻量的操作系统版本来运行操作系统实例。

Docker gained momentum, using Bash automation tool, combinatory badge, accelerator guidance scale, managed deployments to other clouds puppet, chef, Ansible, GIT, packaging concept, for Docker engine, Docker Hub - repository of images public private version control
The container ecosystem is about to explode with the rise of technologies like Docker. The technology for containers have existed for many years, but it is just now that we are finally getting automation software, and tooling that makes managing the container life cycle easier. Supplementary hosting and cloud services business are accepting Linux-based container solutions. In the next section we evaluate and contrast between Docker containers versus virtualization.
Docker获得了势头，使用Bash自动化工具、组合徽章、加速器指导规模、托管部署到其他云木偶、厨师、Ansible、GIT、打包概念、用于Docker引擎、Docker Hub -镜像存储库公共私有版本控制
随着Docker等技术的兴起，容器生态系统即将爆发。容器技术已经存在很多年了，但就在现在，我们终于得到了自动化软件和工具，使管理容器生命周期更容易。补充托管和云服务业务正在接受基于Linux的容器解决方案。在下一节中，我们评估和对比Docker容器与虚拟化之间的关系。

Docker Features: a) It is Light-Weight with negligible overhead (computation/I-o/communications), User layered file system to save space (AUFS/LVM), and it uses a copy-on-write file system to track changes. b) Portable: Can run on any Linux system that supports LXC. Also, Raspberry pi support, OS's (Solaris, OSX, Windows).c) It is self-sufficient: A Docker container encloses all basic components it needs to run, negligible Base OS, Libraries and frameworks, configuration files, Application code and user DB. Challenges include multiplicity of stacks, multiplicity of hardware environments, services and apps should interact appropriately, migrate smoothly and quickly. Separation of concerns inside the container include worries about code, libraries, package manager, apps, data, configuration files, etc. Similarly Outside the container include logging, remote access, monitoring, network configuration, container start, stop, copy, attach, and migrate, etc.

Docker特点：a)它重量轻，开销可以忽略不计（计算/I-o/通信），用户分层文件系统以节省空间（AUFS/LVM），它使用写时复制文件系统来跟踪更改。b)可移植：可以在任何支持LXC的Linux系统上运行。此外，Raspberry pi支持，操作系统（Solaris、OSX、Windows）.c)它是自给自足的：Docker容器包含了它运行所需的所有基本组件、可忽略不计的基本操作系统、库和框架、配置文件、应用程序代码和用户DB。挑战包括堆栈的多样性、硬件环境的多样性、服务和应用程序应适当交互、平稳快速迁移。容器内部的关注点分离包括代码、库、包管理器、应用程序、数据、配置文件等。同样，容器外部包括日志记录、远程访问、监控、网络配置、容器启动、停止、复制、附加和迁移等。

DockerVs Virtualization
Docker containers is lighter than Virtual machines, takes less time to start and we can run a ton of processes on a reasonable sized host where Deploying and scaling is relatively is easy. “Dockerized” applications are completely portable technologies behind Docker. Different users and project groups are properly isolated from each other so that they have no visibility into, or access to, other containers in the system. Fig 6 shows container versus VM's, Fig8 shows Container versus VM's. Kubernetes makes each service easily addressable by any other within the cluster or outside. Kubernetes assigns IP addresses for each Pod as well as for Service, IP for Pods and Virtual IP for Services 
Docker对比虚拟机
Docker容器比虚拟机更轻，启动时间更短，我们可以在大小合理的主机上运行大量进程，在那里部署和扩展相对容易。“Docker化”应用程序是Docker背后的完全可移植技术。不同的用户和项目组相互适当隔离，因此他们无法查看或访问系统中的其他容器。图6显示了容器与虚拟机的关系，图8显示了容器与虚拟机的关系。Kubernetes使每个服务都可以轻松地被集群内或集群外的任何其他服务寻址。Kubernetes为每个Pod以及服务、Pod的IP和服务的虚拟IP分配IP地址
1.2	Docker Container Security Analysis
Docker容器安全性分析
 security pattern for Docker using Kubernetes. The successes and failure of Dev-Ops both in tooling and in culture has provided a separation of concerns where the hard-ops folks will manage the hardware and networking infrastructure underneath. Docker changes this by being the Dev-Ops tool that imposes a security model on its users and makes tweaking that model easy and convenient.
Containers, like rooms, are invaluable in keeping close, but separate, and - those processes that for a number of reasons must be collocated inside the same machine or VM. The loose separation between containers is not a bug, but an asset. Unfortunately, what have not existed as a strong movement is Dev-Sec-Ops (or Sec-Dev-Ops). Cloud Stack helps developers create multi-tenant, versatile cloud services and scale cloud projects. It was initially coded as A6, an ellipsis for automated, audit, assertion, assessment and assurance API.
使用Kubernetes的Docker安全模式。Dev-Ops在工具和文化方面的成功和失败提供了一个分离的担忧，即硬运营人员将管理底层的硬件和网络基础架构。Docker改变了这一点，成为Dev-Ops工具，它将安全模型强加给其用户，并使调整该模型变得简单方便。

容器，就像房间一样，在保持紧密但分开方面是非常宝贵的，而且-那些由于多种原因必须在同一台计算机或虚拟机中并置的进程。容器之间的松散分离不是一个错误，而是一个资产。不幸的是，作为一个强大的运动并不存在的是Dev-Sec-Ops（或Sec-Dev-Ops）。Cloud Stack帮助开发人员创建多租户、多功能的云服务，并扩展云项目。它最初被编码为A6，一个用于自动化、审计、断言、评估和保证API的省略号。


Security implementation of containers is mainly uncharted pitch. The security characteristics of containers system can be compared to the pedals of the Linux operating system and hypervisors. Depending on the use cases of service and business type obligatory security, monitoring controls, and risk management of Linux containers are mature enough to be used as private and public PAAS. In assorted hybrid environments across multiple, multilevel, multilateral trust levels, security precinct or potential antagonistic tenants additional safeguards should be configured and designed based on every emerging threats, attacks, and vulnerabilities. Containers run by Docker are valuable in resource seclusion compared to hypervisors as claimed by dot-Cloud but inherit complexity. Based on Gartner report and recommendation containers are almost on par with the Linux OS and hypervisors in secured mission, supervision, monitoring, configuration, governance and regulating. As Gartner research claim Docker container system frustrates whilst secure supervision and administration, and to support common controls for privacy, reliability, integrity and accessibility, tight security. In various cases, containers run on top of virtualized systems - be it hypervisors or cloud infrastructure or VM running inside the containers, in such cases, containers gives a plus level of virtualization and security on top of the operating environment but, they need to be individually administered. Beginning with customary deployments in anticipation of the de facto principles for container supervision and SDN's, Network VM's, and using network sandboxes in containerized ecosystem are devised.
Docker based OS virtualization has changed constructively for container security and controls for docker resource isolation which include firmware, software or hardware, embedded virtual ware, supported techniques isolate applications per host or per container basis [Applications and process, Tenants isolation, VM isolation, Network, storage device, attack or facade vector, kernel isolation] etc. are no good answers and no de facto standards for building secure container systems using compartmental security and Chinese model based security implementation. Scalability and elasticity of Docker virtualization PAAS features should not impede security of logging and validating trivial security methods. Docker daemon runs as multiple processes, each running on a single Linux OS host kernel. Multiple processes, each running in a dedicated Docker container on the same host kernel. Multiple applications keep running in a VM on the same hypervisor. Multiple Docker containers with same trust level or regulatory category per OS instances with hypervisor. Docker and containers cannot succeed to from the hypervisor what they lack most: secure supervision and management features, and controls. But the hypervisor append complexity of managing separately and may cause friction with, sprouting SDN's for containerized environments
容器的安全实现主要是未知的间距。容器系统的安全特性可以与Linux操作系统和虚拟机管理程序的踏板相提并论。根据服务和业务类型的使用案例，Linux容器的监控控制和风险管理已经足够成熟，可以用作私有和公共PAAS。在跨多个、多层次、多边信任级别、安全区或潜在敌对租户的各种混合环境中，应根据每一个新兴威胁、攻击和漏洞配置和设计额外的保护措施。与dot-Cloud声称的虚拟机管理程序相比，Docker运行的容器在资源隔离方面很有价值，但继承了复杂性。根据Gartner的报告和建议容器在安全任务、监督、监控、配置、治理和监管方面几乎与Linux操作系统和虚拟机管理程序相当。正如Gartner的研究所声称的那样，Docker容器系统在安全监督管理的同时，支持隐私、可靠性、完整性和可访问性、严格安全性的共同控制。在各种情况下，容器运行在虚拟化系统之上-无论是虚拟机管理程序、云基础架构还是运行在容器内的虚拟机，在这种情况下，容器在操作环境之上提供了更高的虚拟化和安全性，但它们需要单独管理。从预期容器监管和SDN的事实上原则的习惯部署开始，设计了Network VM的部署，并在容器化生态系统中使用网络沙箱。
基于Docker的操作系统虚拟化在容器安全和Docker资源隔离控制方面发生了建设性的变化，包括固件、软件或硬件、嵌入式虚拟软件、支持的技术隔离每个主机或每个容器的应用程序[应用程序和进程、租户隔离、虚拟机隔离、网络、存储设备、攻击或门面向量、内核隔离等都不是很好的答案，也没有使用分区安全和基于中国模型的安全实现来构建安全容器系统的事实上标准。Docker虚拟化PAAS功能的可扩展性和弹性不应妨碍日志记录和验证琐碎安全方法的安全性。Docker守护程序作为多个进程运行，每个进程运行在单个Linux操作系统主机内核上。多个进程，每个进程运行在同一主机内核上的专用Docker容器中。多个应用程序在同一虚拟机管理程序上的虚拟机中持续运行。每个具有虚拟机管理程序的操作系统实例具有相同信任级别或监管类别的多个Docker容器。Docker和容器无法成功地从虚拟机管理程序中获得它们最缺乏的东西：安全的监督和管理功能以及控制。但是，虚拟机管理程序增加了单独管理的复杂性，并可能会与容器化环境中的SDN产生摩擦


1.3	Results and Discussions
结论描述
To come out with a sole structural design for cloud services linking solution to security and privacy issues and improved design of security algorithmic solutions of combined cloud services involving security & privacy issues with unique architecture, and framework. Provide solution on Jurisdiction and legal issues. Docker Containers offer an added stratum of defense by separating the application and the host, and the between applications. It has process restrictions, device & file restrictions, application image security, docker allows container images to originate from disk or from a remote registry, and these configuration controls described above are provided as a run-time facility, where isolation, orchestration, scheduling and application management mechanisms are concentrated. This allows users to tune and configure the policies of their containers, based on the user's operational and organizational needs. Other Linux kernel security features modern Linux kernels have many more security constructs in addition to the referred concept of capabilities, namespaces and cgroups. Docker can leverage existing systems like TOMOYO, AppArmor, SELinux and GRSEC, some of which come with security model templates available out of the box for Docker containers. Linux kernels with GRSEC and PAX set. These sets of patches add several kernel-level safety checks, both at compile-time and run-time that attempt to defeat or make some common exploitation techniques more difficult. This is not a Docker specific configuration, but can be applied and be beneficial system wide. To craft something extraordinary multilateral collaborative effort with one's intellectual mindset one must focus on minute details, with cooperation and collaboration. 2p+0.5e=4x i.e. double the productivity (2p) in using half the effort (0.5e) would yield 4 times the result of security (x). Multiple scenarios helps to have good idea in multiple instances collaboration. Having bigger picture in beginning help incremental design at regular intervals and eliminate waste. Step Process a. Visualize b. Discuss c. Contract Discuss, Talk, Converse. Brings out the ulterior, ultra high security benefit, multilateral collaboration clarifies the enforce privacy policies.

为云服务提供唯一的结构设计，将解决方案与安全和隐私问题联系起来，并改进了涉及安全和隐私问题的组合云服务的安全算法解决方案的设计，并具有独特的架构和框架。就管辖权和法律问题提供解决方案。Docker容器通过分离应用程序和主机以及应用程序之间的，提供了额外的防御层。它具有进程限制、设备和文件限制、应用程序映像安全、docker允许容器映像源自磁盘或远程注册表，上述这些配置控制作为运行时设施提供，集中在其中隔离、编排、调度和应用程序管理机制。这使用户能够根据用户的运营和组织需要调整和配置其容器的策略。其他Linux内核安全功能现代Linux内核除了引用的功能、命名空间和cgroup概念外，还有更多的安全结构。Docker可以利用现有系统，如TOMOYO、AppArmor、SELinux和GRSEC，其中一些系统附带了开箱即用的安全模型模板，可用于Docker容器。设置GRSEC和PAX的Linux内核。这些修补程序集添加了几个内核级安全检查，包括在编译时和运行时，这些检查试图击败或使一些常见的利用技术变得更加困难。这不是Docker特定的配置，但可以应用，并在系统范围内有益。要以自己的智力心态制定一些非凡的多边合作努力，就必须以合作与协作的方式专注于微小的细节。2p+0.5e=4x，即事半功倍的生产力(2p)(0.5e)将产生4倍的安全结果(x)。多场景有助于在多实例协作中拥有良好的想法。在开始时，有更大的前景有助于定期进行增量设计，并消除浪费。步骤处理a。可视化b。讨论c。合同讨论，谈话，交谈。带来不可告人的、超高的安全利益，多边合作澄清了执行隐私政策。
1.4	Areas to Contribute
领域贡献

Docker Security documentation standards, security code contribution, cross port, cross pile, cross technology support with hybrid of architectural support, compute supports random scheduler, resource based scheduling, in development, write filters, and memory threshold and multi-master monitoring in the ideation phase. Network open V switch collate L2, L3 layers network bridge in two's SDN's capabilities which is distributed switch storage support for policies, any storage, special storage array in a fine-grained way. Third generation application write micro service, using web service layer, tomcat machines, stateless API's, Tomcat Mesos container same reaction status preserved Apache web server, load balancing, DB server, port number, rigid infrastructure, touch configure files, service registry. Immutable infrastructure, high fidelity structure, builds a stack. Debt in container, OS kernel attack memory, CPU consumes offline automatically scaled. Memory utilization going up check, CPU utilization going up monitor, load balancer traffic measure, back-end many containers ─ underlining infrastructure, cannot be auto scaled, batch processing ─ out of scale weight, DOS, DDOS prevention [104]. Miniaturized architecture, event driver, lambda architecture protocol, front end web server, app server, back end container, infrastructure, micro service architecture include persistent layer, key value layer. Parallel distributed micro service oriented high performance, enterprise system scaled, flexible elastic container, bullet security, request location measure latency, check agents deployed stack, layer of network stack secure productive scheduling, shell manager to monitor. Solution architecture role built in block, networking intricacy routers, helpline, shared security model, segmentation, analyze access pattern used via network .

Docker安全文档标准、安全代码贡献、交叉端口、交叉堆、交叉技术支持与体系结构支持的混合、计算支持随机调度器、基于资源的调度、开发中、写入过滤器、想法阶段的内存阈值和多主监控。网络开放V交换机在两个SDN的功能中整理了L2、L3层网络桥，即分布式交换机存储支持策略、任何存储、特殊存储阵列，以细粒度的方式。第三代应用程序编写微服务，使用Web服务层，tomcat机器，无状态API的，Tomcat Mesos容器相同的反应状态保留Apache web服务器，负载平衡，DB服务器，端口号，刚性基础架构，触摸配置文件，服务注册表。不可变的基础架构，高保真结构，构建堆栈。容器中的债务，操作系统内核攻击内存，CPU消耗离线自动扩展。内存利用率上升检查、CPU利用率上升监控、负载平衡器流量测量、后端许多容器-强调基础架构，无法自动扩展，批处理-超出扩展权重，DOS, DDOS预防[104]。小型化架构、事件驱动、lambda架构协议、前端Web服务器、应用服务器、后端容器、基础设施、微服务架构包括持久层、键值层。面向并行分布式微服务的高性能、企业系统可扩展、灵活弹性容器、子弹安全、请求位置测量延迟、检查代理部署堆栈、网络堆栈层安全生产调度、shell管理器监控。内置在块中的解决方案体系结构角色、网络复杂路由器、帮助热线、共享安全模型、分段、分析通过网络使用的访问模式。

